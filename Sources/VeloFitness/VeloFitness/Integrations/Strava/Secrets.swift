// Secrets.swift — gitignored, never commit credentials.
// Copy Secrets.swift.example → Secrets.swift and fill in your values.
enum StravaSecrets {
    static let clientID     = "YOUR_STRAVA_CLIENT_ID"
    static let clientSecret = "YOUR_STRAVA_CLIENT_SECRET"
}
